Final frozen GPIO table
GPIO	Assigned function
GPIO1	Battery ADC
GPIO2	Left RS485 TX
GPIO4	Level confirmation input 1
GPIO5	Level confirmation input 2
GPIO6	Relay CH1 — Siren
GPIO7	Relay CH2 — Flash
GPIO8	Relay CH3 — Voice / Future
GPIO15	Left RS485 RX
GPIO16	Relay CH4 — Pump
GPIO17	RF danger siren
GPIO18	RF pump
GPIO19	Left RS485 RTS / DE-RE
GPIO21	DYP UART RX
GPIO38	Right RS485 RX
GPIO39	Right RS485 TX
GPIO47	Right RS485 RTS / DE-RE
GPIO48	Config button

Reserved:

GPIO range	Reason
GPIO9–GPIO14	Onboard microSD
GPIO35–GPIO37	N16R8 Octal PSRAM
GPIO40–GPIO42	Onboard LEDs
GPIO0, GPIO3, GPIO45, GPIO46	Strapping / boot-sensitive
GPIO20	Keep unused as native USB D+; do not repurpose casually
GPIO43, GPIO44	UART0 debug / service

Espressif identifies GPIO0, GPIO3, GPIO45, and GPIO46 as ESP32-S3 strapping pins, and states that GPIO33–GPIO37 are occupied when Octal PSRAM is used.

Use this response for Codex:

Approved. Apply the revised left RS485 mapping in one coordinated change.

```cpp
// Left RS485 bus
#define PIN_RS485_LEFT_RX    15
#define PIN_RS485_LEFT_TX     2
#define PIN_RS485_LEFT_RTS   19

// Right RS485 bus: unchanged
#define PIN_RS485_RIGHT_RX   38
#define PIN_RS485_RIGHT_TX   39
#define PIN_RS485_RIGHT_RTS  47
```

Reason:

```text
GPIO35, GPIO36, and GPIO37 cannot be used because the Edgehax ESP32-S3-WROOM-1 N16R8 board uses 8MB Octal PSRAM.

GPIO19 is intentionally repurposed from native USB D- to Left RS485 RTS.
Native USB / USB-JTAG is sacrificed in the deployed firmware profile.
Use the USB-to-UART programming port, Wi-Fi OTA, and local HTTP update for service.
```

Update these files together:

```text
device_profile.h
rs485_rtu_master.cpp
hardware specification
diagnostics diary
wiring documentation
regression tests
```

Do not make piecemeal edits.

After implementation, run these mandatory bench tests:

```text
1. Cold boot at least 20 times with both RS485 modules connected.
2. Confirm no TG1WDT_SYS_RST or PSRAM-related panic.
3. Confirm left bus RX/TX/RTS communication.
4. Confirm right bus RX/TX/RTS communication.
5. Confirm SD card mount and file write/read.
6. Confirm ADC, relays, RF outputs, DYP UART, and config button.
7. Confirm OTA update over Wi-Fi.
8. Confirm USB-to-UART flashing remains available.
9. Confirm native USB is documented as unavailable in field profile.
```

Freeze this mapping only after all tests pass.


One small wording correction: call it the approved candidate frozen mapping until the full bench test passes. After that, update the firmware profile revision from FLOODGUARD-S3-01 to something like:

FLOODGUARD-S3-EDGEHAX-N16R8-02