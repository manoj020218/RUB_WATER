# VPS Scripts

Keep deployment and maintenance scripts here.

## Planned Scripts
- `deploy-backend.ps1` / `deploy-backend.sh`
- `restart-services.ps1` / `restart-services.sh`
- `backup-db.ps1` / `backup-db.sh`
- `rotate-logs.ps1` / `rotate-logs.sh`
- `health-check.ps1` / `health-check.sh`

## Recommended Runtime Commands
- service status checks
- broker health checks
- API health endpoint checks
- DB connectivity checks

All scripts should be idempotent and production-safe.

